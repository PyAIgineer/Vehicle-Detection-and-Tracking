# Sentinel — Requirements
# Install: pip install -r requirements.txt

# API server
fastapi
uvicorn[standard]
python-dotenv

# Computer vision
opencv-python
numpy

# YOLO detection + ByteTrack
ultralytics

# Embeddings (ResNet18)
torch
torchvision

# OCR
easyocr

# LLM (report narrative + Groq Vision)
groq

# Vector DB (ReID)
qdrant-client